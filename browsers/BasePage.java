package browsers;

import org.openqa.selenium.WebDriver;

public class BasePage {
	WebDriver driver;
	public BrowserActions actions;
	
	/*
	 * public BasePage(WebDriver driver) { this.driver = driver; this.actions = new
	 * BrowserActions(driver); }
	 */
}
